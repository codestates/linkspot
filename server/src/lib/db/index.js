const user = require("./user.db")
const direct = require("./direct.db")
const server = require("./server.db")
const channel = require("./channel.db")
const friend = require("./friend.db")
const mention = require("./mention.db")

module.exports = { user, direct, server, channel, friend, mention }
